apiVersion: v1
kind: PersistentVolumeClaim
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{SOLR_VOLUME_SIZE}}
  storageClassName: "{{ OPENSHIFT_STORAGE_CLASS_NAME }}"
