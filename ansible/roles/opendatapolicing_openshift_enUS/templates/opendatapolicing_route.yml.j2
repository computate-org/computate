apiVersion: route.openshift.io/v1
kind: Route
spec:
  host: {{OPENDATAPOLICING_SITE_HOST_NAME}}
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: passthrough
  to:
    kind: Service
    name: opendatapolicing
    weight: 100
  wildcardPolicy: None

