apiVersion: route.openshift.io/v1
kind: Route
spec:
  host: {{COMPUTATE_MEDICALE_SITE_HOST_NAME}}
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: passthrough
  to:
    kind: Service
    name: computate-medicale
    weight: 100
  wildcardPolicy: None

