apiVersion: route.openshift.io/v1
kind: Route
spec:
  host: {{SOUTHERNCOALITION_SITE_HOST_NAME}}
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: passthrough
  to:
    kind: Service
    name: southerncoalition
    weight: 100
  wildcardPolicy: None

